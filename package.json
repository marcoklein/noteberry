{
  "name": "noteberry",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "clean": "rimraf \"./packages/*/dist*\"",
    "prepare": "yarn bootstrap",
    "build": "lerna run build",
    "deploy:docs": "lerna run --scope @deployment/docs deploy:docs"
  },
  "devDependencies": {
    "@types/node": "^17.0.21",
    "copyfiles": "^2.4.1",
    "lerna": "^4.0.0",
    "rimraf": "^3.0.2",
    "typescript": "^4.6.2",
    "ts-node": "^10.7.0"
  }
}
